version: "2"
sql:
  - schema: "db/schema.sql"
    queries: "db/queries"
    engine: "postgresql"
    gen:
      go:
        package: "generated"
        out: "db/generated"
        sql_package: "pgx/v5"
        emit_interface: false
        emit_json_tags: true
        overrides:
          - db_type: "uuid"
            go_type:
              import: "github.com/google/uuid"
              type: "UUID"
